---
name: setup-auth-flow
description: Set up React Navigation auth flows with static or dynamic navigation.
---

# Setting Up Auth Flow

Inspect the navigator shape first.

- If the app uses static config, read `references/static.md`.
- If the app uses dynamic JSX config, read `references/dynamic.md`.

Load exactly one reference file unless the user explicitly asks to compare both approaches.
